## Файлы для DelayedNotifier — отложенные уведомления через очереди  

### 📋 Описание проекта  

Во многих сервисах пользователям нужно напоминать о чём-то не сразу, а через какое-то время.  
Это могут быть уведомления о предстоящих встречах, напоминания о заказе, сообщения в мессенджере  
или email через несколько часов, дней и т.д. Вручную такие вещи не отправляют — этим занимается  
отдельный сервис, который умеет «запоминать», когда и кому нужно отправить сообщение,  
и делает это автоматически.  

В этом проекте реализован как раз такой сервис. Он принимает запросы на создание уведомлений,  
складывает их в очередь (через RabbitMQ), а затем — в нужное время — отправляет. Если что-то  
пошло не так, сервис попробует повторить отправку с задержкой. Это типичный бэкенд-компонент,  
который может использоваться в CRM-системах, образовательных платформах, онлайн-магазинах  
и других проектах.  

### 🖥️ Возможности

Встроенные HTTP-методы:  

  – **POST /notify** — создание уведомлений с датой и временем отправки;  
  – **GET /notify/{id}** — получение статуса уведомления;  
  – **DELETE /notify/{id}** — отмена запланированного уведомления.  

Фоновая обработка очереди уведомлений:  

  – уведомления отправляются в указанное время;  
  – в случае ошибки отправка повторяется (с экспоненциальной задержкой).  

Дополнительно:

  - использование Redis для кэширования и быстрой проверки статуса;
  - простой UI с формой для создания уведомления и отображением созданных уведомлений и их статусов.  
    Это поможет протестировать сервис без ручных curl-запросов и наглядно увидеть, как он работает.  

### 🗂️ Структура проекта  

```bash
├── main.go                       # точка входа, инициализация компонентов
├── Dockerfile                    # сборка образа
├── compose.yml                   # запуск всех сервисов (PostgreSQL, Redis, RabbitMQ)
├── .env                          # переменные окружения для конфигурации
├── go.mod                        # модуль Go и зависимости
├── go.sum                        # контрольные суммы зависимостей
├── readme.md                     # документация проекта
├── pkg/
│   ├── api/                      # обработчики HTTP-запросов (POST, GET, DELETE)
│   ├── cache/                    # работа с Redis (кэширование, прогрев)
│   ├── configuration/            # загрузка конфигурации из .env
│   ├── consumer/                 # консумер RabbitMQ, обработка уведомлений
│   ├── db/                       # взаимодействие с PostgreSQL (модели, запросы)
│   ├── rabbit/                   # клиент RabbitMQ (подключение, публикация)
│   ├── scheduler/                # планировщик, публикующий уведомления в очередь
│   └── server/                   # запуск HTTP-сервера, middleware
└── web/                          # статические файлы веб-интерфейса
```

### 🚀 Быстрый старт

**Требования:**  
- Docker    
- Свободные порты: 8081 (Web), 5432 (PostgreSQL), 6379 (Redis), 5672 и 15672 (RabbitMQ)  

**Docker-образы**  
- postgres:16  
- redis:7  
- rabbitmq:4.0  

**Запуск системы:**  

    docker compose up  

  - После успешного запуска веб-интерфейс в браузере: http://localhost:8081  

## ⚙️ Конфигурация

Файл настроек **.env**  

    ## переменные сервера и api
    SERVICE_HOST_NAME=0.0.0.0         # имя службы (контейнера) в докере  
    SERVICE_PORT=8081                 # порт хоста, на котором работает сервис  
    GIN_MODE=debug                    # переулючатель режима логов  

    ## переменные базы данных
    DB_HOST_NAME=dbPostgres       # имя службы (контейнера) в докере
    DB_PORT=5432                  # порт, на котором сидит база данных
    DB_NAME=db-postgres           # имя базы данных
    DB_PASSWORD=postgres          # пароль базы данных
    DB_USER=postgres              # имя пользователя базы данных
    
    ## переменные кэша
    REDIS_HOST_NAME=dbRedis   # имя службы (контейнера) в сети докера
    REDIS_PORT=6379           # порт, на котором сидит Redis
    REDIS_PASSWORD=           # пароль от БД Redis
    REDIS_DB=0                # номер БД Redis
    REDIS_TTL=600s            # время жизни данных в кэше в секундах
    REDIS_WARMING=24h         # время в часах за которое берём записи для прогрева кэша
    
    ## переменные для RabbitMQ
    RABBIT_HOST_NAME=RabbitMQ     # имя службы (контейнера) в докере
    RABBIT_PORT=5672              # порт, на котором сидит RabbitMQ
    RABBIT_USER=rabbitMQ          # имя пользователя RabbitMQ
    RABBIT_PASSWORD=rabbit        # пароль от RabbitMQ
    RABBIT_VHOST=/                # virtual host, отдельная «база данных» для очередей, по умолчанию /
    RABBIT_QUEUE=notiQueue        # имя очереди сервиса
    
    # переменные консумера
    RETRY_COUNT=3             # количество попыток отправки уведомления
    RETRY_DELAY=100ms         # базовая задержка отправки уведомлений в миллисекундах
    RETRY_BACKOFF=2           # множитель для увеличения задержки отправки уведомлений

    # переменные планировщика
    SCHEDULER_INTERVAL=60s        # интервал сканирования БД в секундах
