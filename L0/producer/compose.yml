version: '3'

services:
  go-producer:  
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.producer.env
    image: my-producer  
    working_dir: /usr/src/app
    volumes:
      - ./:/usr/src/app
    network_mode: "host"