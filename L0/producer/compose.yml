version: '3'

services:
  go-producer:  
    build:
      context: .
      dockerfile: Dockerfile
    image: my-producer  
    working_dir: /usr/src/app
    command: go run main.go
    volumes:
      - ./:/usr/src/app
    network_mode: "host"