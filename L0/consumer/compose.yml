version: '3'

services:
  go-consumer:  
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    image: my-consumer  
    command: ["./consumer"]
    network_mode: "host"
    stop_grace_period: 45s
