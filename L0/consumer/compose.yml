version: '3'

services:
  go-consumer:  
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    image: my-consumer  
    working_dir: /usr/src/app
    command: go run main.go
    volumes:
      - ./:/usr/src/app
    network_mode: "host"
